// use rust_bert::pipelines::ner::NERModel;

// pub fn nlp(input: Vec<String>) -> Result<(), Box<dyn std::error::Error>> {
//     let ner_model = NERModel::new(Default::default())?;
//     let output = ner_model.predict(&input);

//     println!("{:#?}", output);

//     Ok(())
// }